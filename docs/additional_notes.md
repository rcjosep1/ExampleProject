# Additional Notes

- Rename placeholder files once your actual group number is known:
  - `SES230_GroupNumber_FinalReport.ipynb`
  - `zotero/SES230_GroupNumber_ZoteroLibrary.bib`
- If you decide to publish the repository more broadly, replace the current `LICENSE` file with the real license you want to use.
- If your instructor wants the complete raw EarthChem export in GitHub, you will need Git LFS because the main CSV exceeds the standard 100 MB GitHub file limit.

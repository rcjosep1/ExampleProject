# Data

- `raw/` contains source exports and small sample inputs.
- `processed/` contains cleaned outputs generated from the analysis workflow.
- Large raw EarthChem exports are intentionally ignored by git because they exceed normal GitHub size limits.
